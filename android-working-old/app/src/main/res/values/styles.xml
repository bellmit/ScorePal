<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--<item name="android:windowTranslucentStatus">true</item>
        MESSES UP THE SCROLLING IN SPORT SELECT ETC - SCROLLS UNDER THE TITLE -->
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!--<item name="android:windowTranslucentStatus">true</item>
        MESSES UP THE SCROLLING IN SPORT SELECT ETC - SCROLLS UNDER THE TITLE -->
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Widget"/>

    <style name="Widget.ProgressBar.CircularProgressBar" parent="Widget">
        <item name="android:indeterminateOnly">false</item>
        <item name="android:adjustViewBounds">true</item>
        <item name="cpb_progressColor">@color/colorAccent</item>
        <item name="cpb_hasShadow">false</item>
        <item name="cpb_strokeWidth">20</item>
        <item name="cpb_titleColor">@color/colorAccent</item>
        <item name="cpb_subtitleColor">@color/primaryTextColor</item>
        <item name="cpb_backgroundColor">@color/colorPrimaryDark</item>
    </style>

</resources>
