import 'package:flutter/material.dart';

class PlayMatchScreen extends StatelessWidget {
  const PlayMatchScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Play a new match...'),
    );
  }
}
